version: 2

models:
  - name: stg_positions
    columns:
      - name: position_id
        tests:
          - not_null
          - unique

  - name: dim_security
    columns:
      - name: security_id
        tests:
          - not_null
          - unique